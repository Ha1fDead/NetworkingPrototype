{
  "name": "networkingprototype",
  "version": "1.0.0",
  "description": "threejs prototype",
  "main": "app.ts",
  "scripts": {
    "lint": "tslint --project src/client/tsconfig.json & tslint --project src/server/tsconfig.json",
    "compile": "npm run lint && tsc -p ./src/client/tsconfig.json && tsc -p ./src/server/tsconfig.json",
    "dev": "tsc -p ./src/client/tsconfig.json -w && tsc -p ./src/server/tsconfig.json -w",
    "start": "node ./server/cgi/server/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.5.2",
    "@types/websocket": "0.0.39",
    "express": "^4.16.3",
    "websocket": "^1.0.26"
  }
}
