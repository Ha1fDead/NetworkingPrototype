{
  "name": "networkingprototype",
  "version": "1.0.0",
  "description": "threejs prototype",
  "main": "app.ts",
  "scripts": {
    "compile": "tsc && rollup -c",
    "dev": "tsc && rollup -c -w && http-server -S",
    "start": "tsc && rollup -c && http-server -S",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node cgi/server/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.5.2",
    "express": "^4.16.3"
  }
}
